<?php

/**
 * Implements hook_theme().
 */
function custom_migrate_theme($existing, $type, $theme, $path) {

  return [
    'users_companies' => [
      'variables' => ['users' => NULL],
      'template' => 'users-companies', 
    ],
  ];
}

